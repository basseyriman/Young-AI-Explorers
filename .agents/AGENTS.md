# Agent Rules

## Git Workflow
From now henceforth, every time you make a change, implement a feature, or fix a bug, you MUST commit the changes and push them to GitHub. 

Use standard git commands:
- `git add .`
- `git commit -m "[Description of changes]"`
- `git push origin main`
